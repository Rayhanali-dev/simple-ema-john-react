/**
 * 1. Create firebase project;
 * 2. create web app;
 * 3. npm i firebase;
 * 4. save firebase config and export default app;
 * 5. Build => Authentication => get started => enable signIn method;
 * 6. create signup and login routes; 
*/

/**
 * 1. create a context provider inside components;
 * 2. set the children props
 * 3. set context value
 * 4. set the provider
*/

/**
 * HOSTING
 * -------------------------
 * one time per pc
 * firebase login
 * 
 * for each project one time
 * ----------------------------
 * 1. firbase init
 * 2. are you ready to proceed? Yes
 * 3. hosting optional (use space bar for selected )
 * 4. execing project (enter)
 * 5. what do you want to use as your public directory? If i use vite (use dist) or (build)
 * 6. Configure as a single-page app (rewrite all urls to /index.html)? yes
 * 7. continuos deploys with github? No
 * 
 * 
 * for every time deploy
 * ----------------------------------
 * 1. npm run build
 * 2. firebase deploy
  */  